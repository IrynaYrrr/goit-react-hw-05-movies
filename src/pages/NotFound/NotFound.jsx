import noData from '../../assets/noDataFound.png';

const NotFound = () => {
  return <div>
    <img src={noData} alt='...' style={{ width: '50%', heigh: '50%' }} />
  </div>
}

export default NotFound
